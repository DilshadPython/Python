from example import *

rad = int(input('Enter the radius to find the area: '))
print('The area is :', area(rad))

vol = float(input('Enter raduis to find a volume: '))
print('The volume is: ', volume(vol))