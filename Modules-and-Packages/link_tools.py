import tools

'''
References: https://docs.python.org/3/py-modindex.html
We create a models named tools and the current file is linked
tp the models fole named tools. We import the tools and call the
method throw the models to the methods.
'''
print(tools.roll_dice(int(input('Enter a number: '))))
