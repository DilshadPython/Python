This is Operating System commands, we run each files make sure to read before run because some of them when it run
delete the current files are created. May be need to run it again like the commands in get_cwd.py

Where in main.py display all created command are in current linux dir
