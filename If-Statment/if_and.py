
user = 'Student'

is_registered = True

if user == 'Student' and is_registered:
	print('\n  Yes the user already registered.')
else:
	print('\n Not register in the database')


is_registered = False

if user == 'Student' and is_registered:
	print('\n  Yes the user already registered.')
else:
	print('\n The user is not register in the database')